{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd python-rag-service && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd python-rag-service && python main.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
